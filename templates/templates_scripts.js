
/*
// Not verified as working
var addRootsrc = `var elems = document.querySelectorAll('[src]');
	function addRootsrc(el){var src=el.src;
	if(! URL(src)){el.src = "${rooturl}/" + el.src;}}
	elems.forEach(addRootsrc); `;
*/
